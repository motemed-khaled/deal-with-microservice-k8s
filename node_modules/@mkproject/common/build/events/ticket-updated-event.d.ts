import { Subject } from "./subjects";
export interface TicketUpdatedEvent {
    subject: Subject.ticketUpdated;
    data: {
        id: string;
        title: string;
        price: number;
        userId: string;
    };
}
