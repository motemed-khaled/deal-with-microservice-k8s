import { Subject } from './subjects';
export interface TicketCreatedEvent {
    subject: Subject.ticketCreated;
    data: {
        id: string;
        title: string;
        price: number;
        userId: string;
    };
}
